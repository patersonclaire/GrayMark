<section>
  <h3 class="card-title">
    <div class="justify-content-between d-flex">
      <div>
        <%= @first_school_menu.date.strftime("%d %B '%y") %> - <%= (@first_school_menu.date + 4).strftime("%d %B '%y") %>
      </div>
      <div>
        <%= link_to "Create new menu",  new_school_menu_menu_path(@first_school_menu), class: "btn btn-primary"%>
      </div>
    </div>
  </h3>
  <br>
  <br>

  <div class="cards">
    <% @first_week_menus.where(tailored: true).each do |menu| %>
      <div class="card">
        <div class="card-body">
          <br>
          <%# <p class="card-text"><%= school.postcode %>
            <h3>Allergen Free Menu</h3>
          <div>
            <br>
            <%= link_to "View menu", menu_path(menu), class: "btn btn-outline-primary" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</section>

<br>
<br>
<br>

<% unless @second_week_menus.empty? %>
  <section>
    <h3 class="card-title"><%= @second_school_menu.date.strftime("%d %B '%y") %> - <%= (@second_school_menu.date + 4).strftime("%d %B '%y") %>
    <%= link_to "Create New Allergy Free Menu",  new_school_menu_menu_path(@second_school_menu), class: "btn btn-primary"%>
    </h3>
    <br>
    <br>
    <div class="cards">
      <% @second_week_menus.where(tailored: true).each do |menu| %>
        <div class="card">
          <div class="card-body">
            <br>
            <%# <p class="card-text"><%= school.postcode %>
              <h3>Allergen Free Menu</h3>
            <div>
              <br>
              <%= link_to "View menu", menu_path(menu), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>

  <br>
  <br>
  <br>
<% end %>

<% unless @third_week_menus.empty? %>
  <section>
    <h3 class="card-title"><%= @third_school_menu.date.strftime("%d %B '%y") %> - <%= (@third_school_menu.date + 4).strftime("%d %B '%y") %>
    <%= link_to "Create New Allergy Free Menu",  new_school_menu_menu_path(@third_school_menu), class: "btn btn-primary"%>
    </h3>
    <br>
    <br>
    <div class="cards">
      <% @third_week_menus.where(tailored: true).each do |menu| %>
        <div class="card">
          <div class="card-body">
            <br>
            <%# <p class="card-text"><%= school.postcode %>
              <h3>Allergen Free Menu</h3>
            <div>
              <br>
              <%= link_to "View menu", menu_path(menu), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<% end %>
